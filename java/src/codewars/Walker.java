package codewars;

public class Walker {
	public static int[] solve(int a, int b, int c, int alpha, int beta, int gamma) {
		double pi = 3.14159265358979323846;
		int[] result = new int[4];
		double aX = a * Math.cos(alpha * Math.PI / 180);
		double aY = a * Math.sin(alpha * Math.PI / 180);
		double bX = (- aX) * b / a;
		double bY = 0;
		double cX = 0;
		double cY = 0;
		
		
		return result;
	}
}
